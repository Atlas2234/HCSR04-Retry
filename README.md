# HCSR04-Retry
Redoing HCSR04 sensor with functioning code. Creates a kernel module that registers a character device when installed. Uses GPIO pins for trigger and echo functionality, as well as file operation support for the character device to perform certain functions.
